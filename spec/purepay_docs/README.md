# Pure//Pay documentation snapshot

Run `tools/refresh_purepay_spec.sh` to download the current public developer
SPA and validate its documented endpoint and webhook markers. The curated,
reviewable wire contract is `spec/purepay_schema.json`.

